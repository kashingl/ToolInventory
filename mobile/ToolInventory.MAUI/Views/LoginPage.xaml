<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:ToolInventory.MAUI.ViewModels"
             x:Class="ToolInventory.MAUI.Views.LoginPage"
             x:DataType="vm:LoginViewModel"
             Title="Sign In"
             BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#1C1C1E}">

    <ScrollView>
        <VerticalStackLayout Padding="32" Spacing="16" VerticalOptions="Center">
            <Image Source="dotnet_bot.png" HeightRequest="80" />
            <Label Text="Tool Inventory" FontSize="28" FontAttributes="Bold" HorizontalOptions="Center" />
            <Label Text="Sign in to continue" FontSize="14" TextColor="Gray" HorizontalOptions="Center" />

            <Label Text="{Binding ErrorMessage}"
                   TextColor="Red"
                   IsVisible="{Binding ErrorMessage, Converter={StaticResource StringNotEmptyConverter}}" />

            <Border StrokeShape="RoundRectangle 8" Padding="12,0">
                <Entry Placeholder="Email" Text="{Binding Email}" Keyboard="Email" ReturnType="Next" />
            </Border>

            <Border StrokeShape="RoundRectangle 8" Padding="12,0">
                <Entry Placeholder="Password"
                       Text="{Binding Password}"
                       IsPassword="True"
                       ReturnType="Go"
                       ReturnCommand="{Binding LoginCommand}" />
            </Border>

            <Button Text="Sign In"
                    Command="{Binding LoginCommand}"
                    IsEnabled="{Binding IsLoading, Converter={StaticResource InvertedBoolConverter}}"
                    BackgroundColor="{AppThemeBinding Light=#6200EE, Dark=#BB86FC}"
                    TextColor="White"
                    CornerRadius="8"
                    HeightRequest="50" />

            <ActivityIndicator IsRunning="{Binding IsLoading}"
                               IsVisible="{Binding IsLoading}"
                               HorizontalOptions="Center" />

            <Label HorizontalOptions="Center">
                <Label.FormattedText>
                    <FormattedString>
                        <Span Text="Don't have an account? " />
                        <Span Text="Register" TextColor="{AppThemeBinding Light=#6200EE, Dark=#BB86FC}">
                            <Span.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding GoToRegisterCommand}" />
                            </Span.GestureRecognizers>
                        </Span>
                    </FormattedString>
                </Label.FormattedText>
            </Label>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
