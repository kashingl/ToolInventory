<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:ToolInventory.MAUI.ViewModels"
             xmlns:dto="clr-namespace:ToolInventory.Shared.DTOs;assembly=ToolInventory.Shared"
             x:Class="ToolInventory.MAUI.Views.MaintenancePage"
             x:DataType="vm:MaintenanceViewModel"
             Title="Maintenance">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Add" Command="{Binding AddRecordCommand}" />
    </ContentPage.ToolbarItems>

    <RefreshView Command="{Binding LoadCommand}" IsRefreshing="{Binding IsLoading}">
        <CollectionView ItemsSource="{Binding Records}" ItemSizingStrategy="MeasureAllItems">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="dto:MaintenanceRecordDto">
                    <Border Style="{StaticResource AppCardBorderStyle}">
                        <VerticalStackLayout Spacing="4">
                            <Label Text="{Binding ToolName}" Style="{StaticResource CardTitleLabel}" />
                            <Label Text="{Binding Description}" LineBreakMode="WordWrap" />
                            <Label Style="{StaticResource MutedMetaLabel}">
                                <Label.FormattedText>
                                    <FormattedString>
                                        <Span Text="{Binding Date, StringFormat='{0:dd/MM/yyyy}'}" />
                                        <Span Text=" · " />
                                        <Span Text="{Binding PerformedBy}" />
                                    </FormattedString>
                                </Label.FormattedText>
                            </Label>
                        </VerticalStackLayout>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </RefreshView>
</ContentPage>
